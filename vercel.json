{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm run build:vercel:function && pnpm run db:migrate && pnpm run build:vercel",
  "outputDirectory": "dist/public",
  "functions": {
    "api/trpc/[...path].js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/:path((?!api(?:/|$)).*)",
      "destination": "/index.html"
    }
  ]
}
